# CLAUDE.md

Заметки для Claude Code по проекту tu4ka.

## Обзор

Сервис приёма и хранения данных датчика воздуха «тучка» — это стандартный
airRohr (прошивка NRZ-2024-135/RU, ID 2998975): SDS011 (пыль PM2.5/PM10) +
BME280 (температура, влажность, давление). Заменяет умершее родное облако
(api.beta.armaqi.org).

- Датчик: `http://192.168.27.8/` в локальной сети; каждые 60 с сам POST-ит
  измерение штатным механизмом «отправка на собственный API» (basic auth).
  Параллельно шлёт на sensor.community и madavi.de.
- Сервер: VPS `178.160.230.131` (Ubuntu 24.04, 2 ГБ RAM), ssh-алиас `tu4ka`
  (root, ключ `~/.ssh/tu4ka`). FastAPI + uvicorn на порту 80, systemd-юнит
  `tu4ka`, SQLite `/var/lib/tu4ka/tu4ka.db` (WAL).
- Веб-морда: `http://178.160.230.131/`; API: `/api/v1/current`,
  `/api/v1/history?period=24h|7d|30d|12m|all&tz_offset=<мин>`, `/healthz`,
  OpenAPI — `/docs`. Периоды скользящие (последние 24 часа / 7 дней / 30 дней /
  12 месяцев / вся история от первого измерения), а не календарные; `tz_offset` —
  пояс клиента, минут к востоку от UTC, по умолчанию 0 (UTC), сдвигает сетку
  корзин так, чтобы суточные и более крупные корзины (30d/12m/all) стояли на
  местных полуночах.

Планы: REST API для Android-клиента (база — `/api/v1/*`), телеграм-бот
с уведомлениями о плохом воздухе.

## Команды

```bash
.venv/bin/python -m pytest -q           # тесты (venv из requirements-dev.txt)
./deploy/deploy.sh                      # ручной деплой (rsync + remote_setup.sh)
ssh tu4ka journalctl -u tu4ka -f        # логи сервиса
ssh tu4ka systemctl status tu4ka        # состояние сервиса
curl http://178.160.230.131/healthz     # быстрая проверка живости
gh run list --limit 5                   # прогоны CI
```

**Деплой автоматический**: push в `main` запускает `.github/workflows/ci.yml` —
джоб `test` (py_compile + pytest), затем `deploy`, который зовёт тот же
`deploy/deploy.sh` с `TU4KA_HOST=root@178.160.230.131`. Красные тесты деплой не
пускают. `deploy.sh` остаётся для ручного/аварийного прогона.

Линтера нет. Тесты — pytest, лежат в `tests/`, 238 штук.

## Требования и приёмка

Свод действующих требований со стабильными ID (P — push, C/H/Z/D — API,
A — AQI, U — веб-морда, O — эксплуатация) — `wiki/pages/requirements.md`.
Меняешь поведение — правь его вместе с кодом.

Ручную приёмку по этому списку делает субагент `tester`
(`.claude/agents/tester.md`): поднимает локальный стенд на сид-базе, дёргает
API, смотрит дашборд в браузере, сверяет с чек-листом и докладывает
расхождения, ничего не чиня. Звать после реализации задачи, до того как
считать её готовой.

## Архитектура

- `server/main.py` — всё приложение FastAPI: приём push (`POST /api/v1/push`),
  маппинг `sensordatavalues` (SDS_P1→pm10, SDS_P2→pm25, BME280_*/BMP280_*;
  давление приходит в Па, храним в гПа), SQLite-схема, history-агрегация.
  Окна `/history` скользящие: `period_window()` считает `(start, end)` от
  сетки корзин, сдвинутой на `tz_offset` (`end` — начало корзины, в которую
  попадает `now`; для `all` начало окна — корзина первого измерения). Корзины
  без измерений отдаются с null и `n=0` — плотная сетка, ось времени клиента.
  Каждая точка несёт `aqi_lo`/`aqi_hi` (AQI по минимуму/максимуму PM в корзине,
  `aqi_hi` точен, `aqi_lo` — нижняя оценка) и `<метрика>_lo`/`_hi` для всех
  пяти метрик (pm25, pm10, temperature, humidity, pressure — просто min/max
  внутри корзины) — фронт рисует по ним ленту разброса вокруг линии AQI или
  выбранной метрики.
- `server/aqi.py` — расчёт US EPA AQI: breakpoints PM2.5/PM10 (редакция 2024,
  источник — AirNow Technical Assistance Document, Table 6) + NowCast (12-часовое
  взвешенное среднее). Чистые функции без зависимостей; `/current` отдаёт NowCast-AQI,
  `/history` — мгновенный AQI (и его lo/hi разброс) по корзине. Переиспользуется
  будущими ботом/клиентом.
- `server/static/index.html` — веб-морда одной страницей, без сборки и внешних
  зависимостей. Графики (линия AQI + лента разброса, спарклайны метрик) — свой
  SVG на клиенте, Chart.js не используется. PM2.5/PM10/AQI красятся в цвет
  текущей AQI-категории; температура/влажность/давление — в свой фиксированный
  цвет (терракотовый/синий/фиолетовый, `METRIC_COLORS`/`metricColor()`),
  одинаковый в обеих темах. Шрифты (Manrope, JetBrains Mono) вендорены в
  `server/static/fonts/` (`@font-face`, без Google Fonts CDN). Тёмная/светлая
  тема — `prefers-color-scheme`. AQI-герой с вердиктом и рекомендациями,
  «худшие часы» и сводка за 24 часа считаются на клиенте из
  `/api/v1/current` и `/api/v1/history`.
- `deploy/remote_setup.sh` — идемпотентная настройка сервера; креды push
  генерируются один раз в `/etc/tu4ka/env` (TU4KA_PUSH_USER/TU4KA_PUSH_PASS).
- Код на сервере: `/opt/tu4ka/app`, venv `/opt/tu4ka/venv`.
- `tests/` — pytest. `conftest.py` подменяет `main.DB_PATH`/`PUSH_*` через
  `setattr`, а не переменными окружения: `main.py` читает их в глобалы на импорте,
  так что `setenv` опаздывает. `TestClient` — только как контекст-менеджер, иначе
  не отработает `lifespan` и в БД не будет схемы. Время в тестах истории заморожено
  фикстурой `frozen_now` — без неё они плавают у границы суток.
- `.github/workflows/ci.yml` — CI/CD. Ключ деплоя лежит в секрете репозитория
  `TU4KA_SSH_KEY` (отдельный ed25519, в `authorized_keys` сервера с префиксом
  `restrict`), host key сервера запиннен прямо в workflow — он публичный, и в git
  его видно под ревью.

Чего не ломать:

- Формат `POST /api/v1/push` — это формат прошивки airRohr, менять нельзя;
  датчик настроен на путь `/api/v1/push`, порт 80, user `tu4ka`.
- Пароль push в `/etc/tu4ka/env` должен совпадать с прошитым в датчике
  (веб-конфиг датчика → «Отправка данных на собственный API»).
- `measurements.raw` хранит исходный JSON — не удалять, это страховка
  для будущих миграций.
- БД лежит вне `/opt/tu4ka` — деплой с `--delete` её не трогает.
- Breakpoints AQI в `server/aqi.py` — из авторитетной таблицы AirNow (EPA), редакция
  2024. Не подгонять «по памяти»: при правках сверяться с первоисточником и держать
  самотест (эталонные точки: PM2.5 12.0→56, 35.4→100; PM10 155→101).
- Корзины `/history` привязаны к сетке `period_window()` (сдвинутой на `tz_offset`),
  а не к эпохе. Инвариант закрыт тестом
  `test_history_buckets_are_anchored_to_tz_shifted_grid_not_epoch`: он специально
  берёт `period=30d, tz_offset=330`, где две привязки расходятся. На `tz_offset=0`
  они совпадают, и регрессию не поймать — не «упрощать» кейс до UTC.
- Шрифты в `server/static/fonts/` — не трогать источник (Google Fonts): при
  обновлении сверяться с реальным CSS-ответом `fonts.googleapis.com`, а не
  переименовывать/пересобирать файлы вручную.

## Wiki

Проект ведёт живую вики в `wiki/` — накопленное знание о системе (матчасть,
внешние источники, roadmap/решения), а не пересказ кода. Паттерн:
[LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f).
Этот раздел CLAUDE.md — schema-документ вики: конвенции и workflow ниже.

Три слоя:

- **`wiki/raw/`** — неизменяемые сырые источники (статьи, PDF, документация
  сторонних сервисов). Только читаются при ingest, не редактируются.
- **`wiki/pages/`** — сама вики: markdown-страницы с матчастью, roadmap и
  решениями, с перекрёстными ссылками. Владелец этого слоя — Claude: создаёт
  и обновляет страницы, не человек.
- **Этот раздел CLAUDE.md** — схема: что и как здесь заведено.

Плюс `wiki/index.md` (каталог всех страниц по категориям, с однострочным
summary — обновляется на каждый ingest) и `wiki/log.md` (хронологический
append-only лог операций, формат записи `## [YYYY-MM-DD] тип | заголовок`,
тип — `ingest | query | lint`).

Операции:

- **Ingest** — в `wiki/raw/` появился новый источник (или человек попросил
  зафиксировать факт/решение) → прочитать, обновить/создать релевантные
  страницы в `wiki/pages/`, обновить `wiki/index.md`, дописать запись в
  `wiki/log.md`.
- **Query** — вопрос про проект сначала ищем по `wiki/index.md`, при
  необходимости идём в исходный код. Если ответ стоит того, чтобы не потерять
  его в истории чата — файлим его как новую/обновлённую страницу в
  `wiki/pages/`.
- **Lint** — по запросу: искать противоречия между страницами, устаревшие
  утверждения (код успел измениться, а страница — нет), страницы-сироты без
  входящих ссылок, важные понятия без своей страницы.
