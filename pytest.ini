[pytest]
# server/main.py импортирует aqi как top-level модуль (import aqi), а не пакетом,
# так что server/ должен лежать в sys.path — иначе тесты его не соберут.
pythonpath = server
testpaths = tests
