# Прод-зависимости. Версии запинены целиком, включая транзитивные: сред стало
# две (прод и бета), и незапиненный `pip install` дал бы им — и CI — разные
# версии библиотек. Бета, чья работа предсказывать прод, тогда перестала бы это
# делать.
#
# Слепок снят с боевого венва (`pip freeze` на сервере) в момент переезда за
# nginx: переезд не должен заодно менять версии. Апгрейд — отдельным осознанным
# коммитом: обновить строки, прокатить на бету, посмотреть, потом в main.
#
# Верхний уровень здесь — fastapi и uvicorn[standard]; остальное затянуто ими.
fastapi==0.139.2
uvicorn[standard]==0.51.0

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
click==8.4.2
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1.1
