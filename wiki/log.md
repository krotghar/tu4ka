# Wiki: лог

Хронологический, append-only. Формат записи: `## [YYYY-MM-DD] тип | заголовок`,
тип — `ingest | query | lint`. `grep "^## \[" wiki/log.md | tail -5` — последние 5 записей.

## [2026-08-03] ingest | Bootstrap вики из CLAUDE.md и README.md

Первичный ingest: развернул структуру вики (`wiki/index.md`, `wiki/log.md`,
`wiki/pages/`, `wiki/raw/`) и заполнил пять страниц матчасти/roadmap из
существующих `CLAUDE.md` и `README.md` — протокол push, AQI/NowCast, инвариант
корзин `/history`, деплой/CI, roadmap и принятые решения. Внешние источники
(AirNow TAD и т.п.) пока не заведены — `wiki/raw/` пуст, это следующий ingest.

## [2026-08-03] ingest | Редизайн дашборда (fe61145) — скользящие окна, SVG-графики

Пока готовил bootstrap-ingest, в `main` прилетел коммит `fe61145` (соседний
агент), меняющий именно то, что только что задокументировал: календарные
периоды `/history` → скользящие окна, Chart.js → свой SVG, шрифты вендорены
локально. Переписал `wiki/pages/history-buckets.md` под новую реальность
(старая версия была бы дезинформацией), дополнил `aqi-nowcast.md` про
`aqi_lo`/`aqi_hi`, завёл `wiki/pages/frontend-redesign.md`, обновил решения в
`roadmap.md` и каталог. Хороший пример, зачем нужен lint: источник (код)
изменился быстрее, чем успела устояться страница о нём.

## [2026-08-03] ingest | Селектор метрик на дашборде (4a41b69) + first_ts в /history

Большой график перестал быть графиком только AQI: карточек стало шесть (AQI
среди них), клик по карточке переключает график. Завёл
`wiki/pages/dashboard-metric-selector.md` — устройство `METRIC_DEFS`, таблица
шагов осей, чем отрисовка AQI отличается от прочих метрик, и главное — что
макет живёт **вне репозитория**, в проекте Claude Design, откуда переносится
руками (плюс два осознанных отступления от него).

Заодно подобрал то, что приехало из параллельной сессии и задевало уже
описанное: `/api/v1/history` теперь отдаёт `first_ts`, по нему фронт прячет
вкладки периодов, на которые истории не набралось (`edadfbb`) — дописал в
`history-buckets.md`. Там же на тот момент зафиксировал, что `aqi_lo`/`aqi_hi`
сервер считает только для AQI — этот факт устарел следующей же записью в
этом логе, см. ниже.

Промежуточные варианты взаимодействия (карточка-«swap», возврат кликом по
заголовку) в вики не тащу — они прожили меньше часа и заменены целиком;
в истории git они есть, но знанием о системе не являются.

## [2026-08-03] ingest | Цвета метрик и диапазоны на графике

По макету из claude.ai/design («Дизайн датчика качества воздуха») раскрасил
спарклайны и линию графика: PM2.5/PM10/AQI — цвет текущей AQI-категории,
температура/влажность/давление — свой фиксированный цвет (терракотовый/синий/
фиолетовый). `/api/v1/history` стал отдавать `<метрика>_lo`/`_hi` для всех
пяти метрик (раньше min/max по PM считались и выбрасывались) — лента разброса
на графике теперь есть у любой выбранной метрики, не только у AQI. Заодно
починил зажим нижней границы оси Y нулём для не-AQI метрик — иначе
отрицательная температура уезжала за кадр.

Эта работа началась и была закоммичена параллельно с «Селектором метрик» —
пришлось руками сводить оба изменения в `renderChart()`/`metricCardHtml()`/
`updateTooltip()` при мёрдже, сохранив обе фичи (шесть карточек с AQI среди
них и цвет/лента на любой метрике). Обновил `wiki/pages/history-buckets.md`,
`wiki/pages/frontend-redesign.md` и переписал раздел «Что меняется вместе с
выбором» в `wiki/pages/dashboard-metric-selector.md` — там было явно написано
«лента разброса только у AQI», что стало неверным.

## [2026-08-03] query | Частичный откат ленты разброса: только AQI и пыль вне 24ч

Увидев результат вживую, откатил часть предыдущей записи: лента разброса
для температуры/влажности/давления убрана совсем (`bandAllowed` в
`renderChart()` — для них всегда `false`), а для пыли (PM2.5/PM10) убрана
только на периоде `24h` — там часовые корзины, лента больше шумит, чем
помогает; на `7d`/`30d`/`12m`/`all` осталась. У AQI не менялось — лента,
как и раньше, есть всегда. Цвет по метрике (терракотовый/синий/фиолетовый и
категорийный для PM/AQI) остался как есть — просили убрать только ленты.
`/api/v1/history` продолжает отдавать `<метрика>_lo`/`_hi` для всех пяти
метрик: бэкенд не трогал, откат чисто во фронтовой отрисовке. Обновил
`wiki/pages/dashboard-metric-selector.md`, `history-buckets.md`,
`frontend-redesign.md`.

## [2026-08-03] query | Лента AQI тоже убрана на 24ч

Живой скриншот показал: у AQI на 24ч лента разброса осталась — предыдущая
запись её не трогала, только пыль/температуру/влажность/давление. Спросил,
это баг или так и надо — оказалось, тоже убрать. `bandAllowed` в
`renderChart()` объединили: теперь AQI и пыль — одно условие, лента есть
везде, кроме периода `24h`; было раньше — у AQI лента на любом периоде, у
пыли только не на `24h`. Обновил таблицу в
`wiki/pages/dashboard-metric-selector.md`.

## [2026-08-03] ingest | Свод требований и субагент-приёмщик

Собрал действующие требования в одну страницу `wiki/pages/requirements.md` —
из CLAUDE.md, README, страниц вики и фактического кода. У каждого требования
стабильный ID (P — push, C/H/Z/D — API, A — AQI, U — веб-морда, O —
эксплуатация) и пометка, чем оно закрывается: автотестом, руками или и тем и
другим. Идея — чтобы ручная приёмка ссылалась на ID, а не пересказывала
поведение заново каждый раз.

Под этот список написан субагент `.claude/agents/tester.md`: поднимает сервер
локально на сид-базе (~400 дней истории — иначе не видны вкладки периодов и
ленты разброса), дёргает API curl'ом, смотрит дашборд в браузере и сверяет с
чек-листом. Рецепт стенда (`uvicorn main:app --app-dir server`, `TU4KA_DB` в
temp, сид-скрипт) проверен вживую перед тем, как попасть в файл агента.
Агент ничего не чинит — только докладывает; прод и датчик для него только на
чтение.

Заодно нашлось расхождение: `README.md` описывал старое API
(`?period=day|week|month`) и упоминал удалённый `chart.umd.js`. По просьбе
пользователя тут же и починил: таблица endpoint'ов и таблица периодов
приведены к реальности, добавлены `tz_offset`, `<метрика>_lo/_hi`, `first_ts`,
вендоренные шрифты, автодеплой и раздел про тесты; Chart.js из структуры
репозитория убран. В `CLAUDE.md` появился раздел «Требования и приёмка» —
указатель на этот список и на субагента.

## [2026-08-03] ingest | Три бага тултипа на графике: цвет, перекрытие, промах точки

Пользователь принёс три скриншота дашборда и три бага к ним.

1. **Цвет не по значению.** Число в тултипе и точка курсора красились через
   `metricColor(key)`, а та для AQI/пыли берёт категорию *текущего* воздуха, а не
   значения под курсором — отсюда зелёное «64 Умеренно» на жёлтом участке линии.
   Линия при этом всегда красилась верно, градиентом по значению. Починено точечно:
   у AQI курсор берёт `CATS[catKey(значение)].line`. `metricColor()` не трогали —
   карточки метрик осознанно ведёт текущий воздух.
2. **Тултип закрывал точку.** У `.tooltip` было жёстко `top: 4px` и никакого
   вертикального позиционирования. Вместо флипа верх/низ выбрали резерв: `.chart-row`
   сдвинут вниз на `--tip-space`, тултип привязан по `bottom: calc(100% + 8px)` —
   при любой своей высоте он стоит над полем и пересечься с точкой не может.
3. **Точка не на линии.** Линия рисуется по сглаженному `midVals` (скользящее
   среднее, окно ≈ n/12), а точка и число брали сырое `p[key]`. Безье ни при чём —
   кривая проходит ровно через опорные точки, расходилось именно сглаживание.
   Сглаженный ряд вынесен в `chartVals`, вертикальный маппинг — в `chartYPct()`,
   и линия, лента, сетка и курсор считают высоту теперь одной функцией.

Попутно по просьбе пользователя тултип упрощён до времени и величины с единицей
(ушли строка диапазона, строка `PM2.5 N · PM10 M` и словесная категория —
категорию передаёт цвет числа), а горизонтальный зажим в `positionTooltip()`
переведён с магических 16–84% на реальную ширину коробки.

Проверка — локальный стенд на синтетической базе (35 дней, ночной пик в жёлтой и
оранжевой зоне при зелёном текущем воздухе плюс выбитая трёхчасовая дыра): прогон
по 200 позициям на всех шести метриках и трёх периодах. Точка ложится на
нарисованный `path` с точностью 0.03% высоты поля, цвет курсора совпадает со стопом
градиента везде, перекрытий поля и вылетов за карточку — ноль. Дважды ловил себя на
ложной тревоге: расхождения давал сам измеритель — `getPointAtLength()` не различает
подпути на разрыве, а координаты в `d` округлены до 0.1.

Затронуто: [dashboard-metric-selector](pages/dashboard-metric-selector.md) (таблица
про тултип переписана, добавлены `chartVals`/`chartYPct` и полоса `--tip-space`),
[requirements](pages/requirements.md) — U8 переписан под новый тултип.

## [2026-08-03] ingest | Тултип вернули к полному составу, но на сглаженных данных

Предыдущий заход ужал тултип до времени и величины — пользователь сказал, что стало
бедно, и задал состав: у AQI — время, число, уровень словами и строка PM2.5/PM10;
у пыли и у температуры/влажности/давления — время и величина с единицей; строка
«диапазон lo–hi» — везде, где для графика нарисована лента разброса.

Существенное следствие: раз значение под курсором теперь сглаженное, сглаженным
должно быть и всё остальное в коробке. Диапазон берётся из `chartLo`/`chartHi` —
это ровно та лента, что нарисована, — а пыль под AQI из `chartPm` (`rolling` тем же
окном `smoothK`). Иначе тултип описывает две разные точки сразу: на исходных
скриншотах бага так и было — «64» при «PM2.5 16,0», хотя AQI(16,0) = 59. После
правки сходится: на стенде «53 Умеренно» при «PM2.5 10,2», и AQI(10,2) = 53.

Полоса `--tip-space` поднята с 72px до 112px: с четырьмя строками коробка выросла
до ~107px и наезжала на заголовок карточки (замерено, было −23px до низа
`.chart-head`). Пустые строки убираются из потока через `.tooltip .tx:empty`.

Проверено на том же стенде: шесть метрик × три периода, 120 позиций на прогон,
плюс светлая тема и узкий вьюпорт (1000px). Точка держится на линии в пределах
0.03% высоты, цвет совпадает со стопом градиента, перекрытий поля, вылетов за
карточку и наездов на заголовок — ноль. Диапазон появляется ровно там, где есть
лента: у AQI и пыли вне `24h`, у остальных метрик никогда.

Затронуто: [dashboard-metric-selector](pages/dashboard-metric-selector.md),
[requirements](pages/requirements.md) — U8.

## [2026-08-03] ingest | S0: пакетирование server/ и разбиение main.py

Первый шаг плана внедрения мульти-тучковой версии. Ограничитель параллельности
для последующих шагов — не логика, а файлы: весь бэкенд лежал в одном
`server/main.py` (314 строк), и любые две параллельные сессии конфликтовали бы
на каждом мерже. `server/` стал python-пакетом, код разведён по файлам-владельцам:
`db.py`, `auth.py`, `history.py`, `routes/{push,api}.py`; `main.py` остался
сборкой приложения. `quality.py` и `geo.py` заведены пустыми каркасами под
следующие шаги.

Чистый рефакторинг: поведение не менялось, и это проверено объективно —
`/openapi.json` совпадает с версией до разбиения байт в байт (первая попытка
дала расхождение в описании `/api/v1/history`, потому что докстринг с контрактом
ответа уехал в `history.py`; вернули в обработчик — он и есть публичная
документация эндпоинта). 238 тестов зелёные, самотест AQI зелёный.

Две вещи, которые пришлось выяснить эмпирически, а не вывести из документации:

- `rsync --delete` чистит лишнее **только внутри передаваемых каталогов** и до
  уровня `app/` не достаёт. Первая версия правки деплоя оставляла бы старую
  плоскую раскладку (`main.py`, `aqi.py`, `static/` прямо в `/opt/tu4ka/app/`)
  на сервере навсегда. Рабочий вариант — передавать корень репозитория с
  фильтром «только `server/`» и `--delete-excluded`, инвариант:
  `app/` содержит ровно `server/`.
- Глобалы `db.DB_PATH` и `auth.PUSH_*` подменяются тестами через `setattr`,
  поэтому обращаться к ним нужно через модуль. При `from … import` авторизация
  push в тестах молча выключилась бы, и тесты на подделку кредов проходили бы
  по неверной причине.

Деплоя не было: работа лежит в ветке `dev`, в `main` не мержилась. Прод крутит
код до разбиения. CI-пайплайн для `dev` и dev-окружение на сервере — отдельная
задача.

Затронуто: [server-package-layout](pages/server-package-layout.md) (новая),
[deploy-cicd](pages/deploy-cicd.md), [history-buckets](pages/history-buckets.md),
[protocol-airrohr-push](pages/protocol-airrohr-push.md),
[requirements](pages/requirements.md) — O1, [roadmap](pages/roadmap.md).

## [2026-08-03] ingest | CI гоняет тесты и на dev, без деплоя

Работа по мульти-тучковой версии идёт в ветке `dev`, а триггер workflow стоял
только на `main` — коммиты в `dev` не проверялись вообще. Добавлен `dev` в
`on.push.branches`; джоб `deploy` уже был отсечён гейтом
`if: github.ref == 'refs/heads/main'`, так что деплоя с `dev` не будет.

Побочный эффект, который пришлось убрать: concurrency-группа была одна на всё
(`deploy-production`), и прогоны с `dev` вставали бы в очередь за прод-деплоем.
Группа стала `ci-${{ github.ref }}` — по ветке. Это безопасно ровно потому, что
деплоит только `main`: разные ветки больше не пересекаются, но два деплоя на
один сервер по-прежнему невозможны. `cancel-in-progress` включён везде, кроме
`main` — устаревший прогон тестов отменять правильно, идущий деплой рвать нельзя.

Затронуто: [deploy-cicd](pages/deploy-cicd.md),
[requirements](pages/requirements.md) — O1.

## [2026-08-03] ingest | Домен amqi.am заведён, DNS в Cloudflare без прокси

Куплен `amqi.am` (регистратор name.am, оплачен до 2027-08-03), заведены три
A-записи на `178.160.230.131`: `amqi.am` и `www.amqi.am` — сайт, `push.amqi.am` —
приём от датчика. Все три проверены: `/healthz` отвечает 200 по каждому имени.
Кода и сервера правка не касается — uvicorn не смотрит на `Host`, так что пока
все три имени отдают одно и то же приложение. Этим снят внешний блокер сессии I1.

Три вещи, которые стоило зафиксировать, потому что из панели они не очевидны:

- Режим `DNS Records` у name.am — это **и есть** Cloudflare: делегирование само
  уехало на `tori.ns.cloudflare.com` / `dave.ns.cloudflare.com`, nameservers
  руками менять не пришлось. Но зона лежит внутри аккаунта name.am, не в нашем
  аккаунте CF: тумблер прокси есть, а cache rules, WAF и API-токен — нет. Значит
  «включить оранжевое облако» позже — это не клик, а заведение своего аккаунта
  CF с переносом NS.
- Проксирование выключено **намеренно**, а не по недосмотру. Оранжевое облако
  ломает выпуск Let's Encrypt по HTTP-01 (проверка не доходит до сервера) и
  подталкивает к режиму Flexible, где CF↔сервер идёт открытым текстом. Плюс путь
  push через CF — лишний слой на маршруте, который не буферизуется и не
  ретраится прошивкой.
- Имена разошлись с планом: было `tu4ka.<домен>` / `push.tu4ka.<домен>`, стало
  `amqi.am` / `push.amqi.am`. Причина — домен куплен специально под проект,
  префикс `tu4ka.` избыточен. Замысел (ingest на отдельном имени, чтобы можно
  было увести на другую машину) сохранён.

Ловушка при проверке: локальный резолвер какое-то время отдаёт `164.90.221.98` —
это парковочная страница name.am, залипшая в кэше с момента до заведения
записей. `dig +trace` и `@8.8.8.8` показывают правду сразу; ошибочно принять это
за «записи не сохранились» очень легко.

Затронуто: [domain-dns](pages/domain-dns.md) (новая),
[deploy-cicd](pages/deploy-cicd.md), [roadmap](pages/roadmap.md),
[requirements](pages/requirements.md) — новое O8.

## [2026-08-04] ingest | I1: nginx впереди, TLS и вторая среда beta.amqi.am

Задача была «хочу дев-версию прода параллельно основной». Упёрлась в то, что
uvicorn держал `0.0.0.0:80` напрямую и второму приложению встать было некуда, —
поэтому бета и сессия I1 (nginx + TLS) приехали одним куском.

Развилки, решённые по ходу:

- **Бета поддоменом `beta.amqi.am`, а не префиксом `amqi.am/beta`.**
  `server/static/index.html` ходит по корневым путям (`/static/…`,
  `fetch("/api/v1/…")`), и на префиксе страница беты молча читала бы API прода —
  худший возможный режим отказа для среды, чья работа предсказывать прод. Плюс
  будущие `__Host-` куки сессий столкнулись бы на общем хосте.
- **Docker обсуждён и отброшен.** `docker compose up -d` пересоздаёт контейнер:
  порт секунду не слушает никто, ровно как `systemctl restart`. Даунтайм он не
  убирает. Единственная его настоящая польза здесь — пин зависимостей, и он
  сделан напрямую в `deploy/requirements.txt`. Решение обратимое.
- **Даунтайм убран socket activation.** Слушающий сокет держит systemd, uvicorn
  получает дескриптор через `--fd 3`; рестарт `.service` не закрывает порт,
  соединения ждут в очереди ядра. Секунда отказов превратилась в секунду
  ожидания. Правило: на выкладке рестартовать только `.service`.
- **Прод-юнит не переименован** в `tu4ka@prod`: `systemctl status tu4ka` разбросан
  по всей документации, а дубль файла для беты остаётся видимым в ревью, в
  отличие от sed-рендера шаблона.
- **certbot только `--webroot`.** Плагин `--nginx` правит тот самый конфиг,
  который деплой перезаписывает каждым прогоном.

Ловушки, на которые пришлось закладываться отдельно:

- Копия `remote_setup.sh` лежит и в `/opt/tu4ka-beta/deploy/`. Запущенная там без
  `TU4KA_ENV`, она по умолчанию считала бы себя продом и переписала бы прод-юнит.
  Закрыто сверкой `TU4KA_ENV` с собственным путём в шапке скрипта.
- Бета, выкаченная раньше прода, оставила бы `:80` без `default_server`, и датчик,
  ходящий на голый IP, улетел бы в редирект беты — с потерей `Authorization`.
  Закрыто явной проверкой наличия прод-конфига в бета-ветке скрипта.
- Голый `curl -fsS` считает успехом 301, поэтому smoke-check без `grep '"ok"'`
  зеленел бы на редиректе вместо живого сервиса.
- `postinst` пакета nginx стартует сервис, а `:80` в момент переезда ещё держал
  uvicorn: без шима `policy-rc.d` упал бы dpkg.
- `add_header` внутри `location` сбрасывает унаследованные заголовки — noindex
  беты пришлось держать только на уровне `server`.
- Локальный fastapi уже разъехался с боевым (0.140.0 против 0.139.2) — ровно то,
  что пин и должен был поймать. Запинен полный слепок с боевого венва, чтобы
  переезд за nginx не менял заодно версии.

Затронуто: [nginx-tls-beta](pages/nginx-tls-beta.md) (новая),
[requirements](pages/requirements.md) — O1/O4/O5/O8 переписаны, добавлены O9–O14,
[deploy-cicd](pages/deploy-cicd.md), [domain-dns](pages/domain-dns.md),
[roadmap](pages/roadmap.md), [index](index.md).

## [2026-08-04] ingest | Переезд прода за nginx: что реально случилось

Выкат сессии I1 на прод. Прошёл со второй попытки, простоя не было ни разу.

**Первая попытка оборвалась** на `systemctl start tu4ka.socket`:
`Socket service tu4ka.service already active, refusing`. systemd не даёт
поднять `.socket`, пока жив `.service`, который тот обслуживает, — а на переезде
старый процесс держит и `:80`, и себя. Отработало это как задумано: `set -e`
уронил скрипт **до** рестарта, старый uvicorn остался на `:80` с тем же pid,
`/healthz` отвечал 200 всё время, датчик не потерял ни одного измерения.

Осталась при этом живая опасность: файл юнита на диске уже новый (`--fd 3`), а
работал старый процесс. Упади он — `Restart=always` поднял бы его на 8000, а
nginx ещё не запущен, и сайт лёг бы. Отсюда правило: обрыв деплоя между
перезаписью юнита и стартом фронта — не «ничего не произошло», а состояние,
которое надо дожать или откатить сразу.

Второй урок оттуда же: признак переезда сначала брался как наличие `--port 80`
в живом юните. На повторном прогоне юнит уже новый, и проверка дала бы
`CUTOVER=0`, сняв автооткат ровно там, где он нужнее всего. Заменено на «сокет
не поднят»; `tu4ka.service.prev` теперь пишется только если его ещё нет.

Ещё мелочь, обжёгшая по ходу: `certbot renew --dry-run`, запущенный по ssh и
переживший обрыв сессии, остаётся процессом-сиротой и держит лок certbot —
то есть блокирует и автоматическое продление по таймеру. Долгие команды на
сервере запускать через `nohup … &`, а не в живой сессии.

Итог проверок: nginx на 80/443, uvicorn только на `127.0.0.1:8000`
(`TriggeredBy: tu4ka.socket`); голый IP и `push.amqi.am` — 200 без редиректа;
`http://amqi.am` → 301 на https; сертификат LE на `amqi.am` + `www.amqi.am`
до 2026-11-02; `certbot renew --dry-run` — успех; датчик доставляет каждую
минуту сквозь nginx.

**Не проверено:** обещание «выкладка без единого отказа» (O13) вживую пока не
измерялось — долбилку `curl` во время деплоя не гоняли. Естественное место для
этой проверки — первый деплой беты.

Затронуто: [nginx-tls-beta](pages/nginx-tls-beta.md).

## [2026-08-04] ingest | Бета поднята; O13 наконец измерен

Шаг 4 плана: `dev` подтянул `main`, пуш поднял бету через CI (прогон зелёный,
деплой 37 с). `beta.amqi.am` резолвится, сертификат выпущен, `X-Robots-Tag:
noindex, nofollow` на месте, оба приложения слушают только loopback (8000 и
8001), путь датчика по голому IP и `push.amqi.am` — по-прежнему 200 без
редиректа.

**O13 измерен и подтверждён.** Долбилка ~2 запроса в секунду через штатный
бета-деплой: 200 из 200 — `200 OK`, ноль отказов соединения, ноль 5xx, максимум
ожидания 0.45 с. Прод в тот же момент (там идёт `reload nginx`) — максимум
0.05 с. Запись «Не проверено» из предыдущей записи закрыта.

Дорога к этому числу состояла из двух собственных ошибок, обе поучительные:

1. Первый прогон дал 160 «отказов» на бете при нуле на проде. Это оказался не
   простой, а **собственный `limit_req`**: на бете он висел на `location /`,
   то есть на всём сайте (`grep -c 'limiting requests' error.log` дал ровно
   164). Асимметрия с продом и была подсказкой — там лимит с первого дня стоит
   только на `/api/v1/push`. Настоящий баг: одна загрузка дашборда — шесть
   запросов, `burst=20` кончается за три обновления, дальше человек ловит 429.
   Бета-конфиг приведён к виду прода.
2. Второй прогон, уже с разбором кодов, записал в отказы **все 200 запросов,
   включая заведомо живой прод**. Причина — `read -r code t < <(curl … -w …)`:
   `curl` не печатает перевод строки, `read` упирается в EOF и возвращает 1,
   хотя значения прочитал. Переписано на `$(...)`, где статус настоящий.

Мораль на будущее: измерение, которое показывает отказ там, где его заведомо
нет, измеряет себя, а не систему. Проверять инструмент на заведомо здоровой
цели — дешевле, чем чинить систему по его показаниям.

Затронуто: [nginx-tls-beta](pages/nginx-tls-beta.md),
[requirements](pages/requirements.md), `deploy/nginx/tu4ka-beta.conf`.

## [2026-08-04] ingest | Зеркало пуша на бету

Наблюдение: дашборд беты зажёг «данные устарели — 11 мин назад». Не поломка,
а конструкция: датчик прошивкой умеет слать на один свой API, он у прода, а
база беты — снимок прода, пересеваемый только на бета-деплое. Отставание
беты равно времени с последней выкладки и растёт линейно.

Решение — `mirror` в nginx: копия `POST /api/v1/push` уходит на `127.0.0.1:8001`,
ответ беты выбрасывается. Разобранные грабли: (1) nginx завершает основной
запрос только после подзапросов, поэтому зависшая бета удлиняет ответ датчику —
таймауты до беты 2 с обязательны; (2) зеркало несёт `Authorization` прода,
поэтому у беты больше нет своего push-пароля — `remote_setup.sh` копирует туда
прод-креды; (3) `mirror` только на пуш-пути, ради чего у `push.amqi.am`
появилась отдельная пуш-локация; (4) старая проверка «бета читает свою базу»
дрейфом `rows` перестала что-либо доказывать.

Порядок выкатки: сначала `dev` (синк кредов), потом `main` (само зеркало живёт
в прод-конфиге).

Затронуто: [nginx-tls-beta](pages/nginx-tls-beta.md),
[requirements](pages/requirements.md) (O2, новое O15),
`deploy/nginx/tu4ka.conf`, `deploy/remote_setup.sh`, `CLAUDE.md`.

## [2026-08-04] ingest | правовой пакет (L1)

Шесть HTML-страниц (`server/static/legal/`, ru+en): условия использования,
политика конфиденциальности, лицензия данных. Раздача — новый
`server/routes/public.py` по стабильным URL `/legal/*` и `/en/legal/*`,
whitelist `(lang, slug) -> файл`, а не склейка пути.

Два решённых по первоисточникам вопроса: (1) ХО-49-Н даёт точные сроки —
5 дней на удаление, 3 рабочих дня на исправление (ст. 20); уведомление
уполномоченного органа (ст. 23) не требуется, пока не обрабатываются особые
категории данных. (2) У sensor.community лицензия данных подписана как ODbL
1.0, а ссылка ведёт на DbCL 1.0 — расхождение названия и текста; мы выбрали
CC BY 4.0 и держим подпись и ссылку согласованными.

Дашборд получил дисклеймер `indicative measurements` в герое (не в подвале)
и три новые ссылки в подвале. Политика конфиденциальности фиксирует контракт
удаления аккаунта как задание для будущего S6.

Затронуто: [legal-package](pages/legal-package.md) (новая),
[requirements](pages/requirements.md) (новая секция L1-L6),
`server/routes/public.py`, `server/static/legal/`,
`server/static/index.html`, `tests/test_legal.py`, `CLAUDE.md`.

## [2026-08-04] ingest | профиль суток /api/v1/hours: «худшие часы» за неделю

Датчик простоял сутки без данных, и блок «худшие часы» на дашборде выродился в
два столбика из двадцати четырёх. Причина конструктивная: блок считался на
клиенте из одного окна `/history?period=24h`, где на каждый час суток приходится
ровно одна корзина, — то есть был не профилем суток, а 24-часовой лентой,
переразложенной по часам.

Профиль переехал на сервер отдельной ручкой `GET /api/v1/hours?tz_offset=`:
24 часа местного времени, каждый усреднён по последним 7 суткам (окно кончается
на границе текущего часа по той же tz-сдвинутой сетке, что и корзины истории).
Час несёт `aqi` по средним PM часа (как корзина `/history`, а не среднее от
AQI), `pm25`, `pm10`, `n` и `days`; конверт — `days_covered`, им фронт
подписывает блок «среднее за N дней». Почасовой гранулярности глубже 24 ч в
`/history` не было и нет: у `7d` корзина уже 3 часа.

Окно оставили фиксированным (7 суток, а не «7 суток с данными»): молчание
дольше недели честно опустошает блок («Пока мало данных»), вместо того чтобы
выдавать позапрошлую неделю за нынешнюю.

Попутно разъехавшаяся сводка за 24 часа: заметка «Максимум N в HH:00» брала час
из усреднённого по часам ряда, а само N — из сырых корзин; теперь и то и другое
из одной корзины.

Затронуто: [history-buckets](pages/history-buckets.md) (новый раздел),
[requirements](pages/requirements.md) (новое H8, правки U10 и U14),
[frontend-redesign](pages/frontend-redesign.md), `server/history.py`,
`server/routes/api.py`, `server/static/index.html`, `tests/test_main.py`,
`CLAUDE.md`.

## [2026-08-04] ingest | I2: бэкапы и алерты

Сессия I2 плана волн. До неё у сервиса не было ни одной копии данных и ни
одного способа узнать о поломке — базой был единственный файл на единственном
диске единственной машины.

Сделано: ежедневный бэкап прода (`VACUUM INTO` из базы в `mode=ro` →
`integrity_check` по файлу копии → `zstd` → ротация 7+6 → офсайт в
S3-совместимое хранилище) и алерты (тишина датчика в телеграм + внешний
dead-man's switch на healthchecks.io). Обе половины — прод-онли.

Решения, отличающиеся от карточки артефакта:

- **Базовая карта отложена в S9 целиком.** В I2 она стояла ради одной
  блокирующей проверки — отдаёт ли Starlette Range. Проверка выполнена здесь
  и пройдена: `curl -r 0-99` по вендоренному шрифту даёт `206` +
  `Content-Range: bytes 0-99/24836`, и от uvicorn, и через nginx по HTTP/2.
  Риск снят, а сам файл до волны 3 — мёртвый груз на диске, занятом на 54%.
  Заодно поймана ошибка в самой карточке: `mount("/tiles", …)` на
  `/var/lib/tu4ka/` отдал бы наружу `tu4ka.db`. Каталог должен быть отдельный.
- **Офсайт делается штатным `curl --aws-sigv4`** (на сервере 8.5). Ни
  `awscli`, ни `rclone` ради одной загрузки в день не нужны.
- **Причина «алерты только на проде» изменилась по ходу.** План писался,
  когда база беты была замороженным снимком и `MAX(ts)` в ней не рос. За время
  сессии приехало зеркало пуша, и данные там стали живыми. Вывод устоял, но
  обоснование теперь другое: сломается зеркало — бета замолчит при здоровом
  проде и даст ложную тревогу.

Что осталось человеку: завести бота у @BotFather, проверку на healthchecks.io
и бакет с ключом на запись; вписать значения в `/etc/tu4ka/env` (ключи
заведены пустыми, пустое = функция выключена, деплой не падает).

**Не проверено на живом на момент записи:** прогон бэкапа, доставка в телеграм,
загрузка офсайта и первый прогон чек-листа восстановления. Всё это требует
заполненных кредов.

Организационное: работа шла в общем дереве с параллельной сессией, и её
`git add -A` утянул шесть файлов I2 в коммит про зеркало пуша. Разобрано через
`reset --soft` до пуша, коммиты разделены. В общем дереве `git add -A` —
опасная команда.

Затронуто: [backup-restore](pages/backup-restore.md), [alerting](pages/alerting.md),
[requirements](pages/requirements.md) (O16–O20), [roadmap](pages/roadmap.md).

## [2026-08-04] ingest | I2 проверен на живом; прод отложен до релиза

Дополнение к предыдущей записи. Принято правило: **до отдельного релизного
этапа на прод не выкатывается ничего**, вся работа идёт в `dev` → бета. Релиз
будет, когда выполнен весь план волн.

Следствие, которое надо помнить: всё, что загейчено `[ "$ENV" = prod ]`
в `remote_setup.sh`, до релиза не исполняется ни разу. Сейчас это бэкапы
и алерты — то есть **боевая база всё это время остаётся без копий**, включая
момент миграции S1, которая перестраивает `measurements` целиком. К вопросу
стоит вернуться до S1, а не после.

Поэтому I2 проверен на бете — её база теперь по размеру и форме совпадает
с боевой (снимок прода плюс зеркало пуша). Прод при проверке не задет:
временный каталог, база открыта `mode=ro`, юниты не ставились.

Что подтвердилось:

- бэкап проходит целиком: `VACUUM INTO` → `integrity_check ok` → `zstd`;
- **сверка данных сошлась**: снимок насчитал 14 868 строк, `/healthz` беты
  отдал `rows: 14868`;
- сжатие оказалось втрое лучше расчётного — 12.1 МБ в **752 КБ**, 16:1.
  Прикидка «пара мегабайт на копию» была пессимистичной, страница поправлена
  на измеренное;
- ротация удаляет правильное: из 11 файлов остаются 7 самых свежих;
- алерт видит тишину и считает её верно, а при живых данных молчит;
- `healthz_ok` → пинг отрабатывает на живом эндпойнте;
- **токен в журнал не утекает**: при заведомо неверном токене в выводе только
  `телеграм: HTTP 401`, самого токена нет;
- неотправленное сообщение не двигает состояние — `state.json` не создаётся.

Не проверено и не может быть до релиза: установка таймеров, реальная доставка
в телеграм, загрузка офсайта и чек-лист восстановления — всё это требует
прод-деплоя и заполненных кредов.

Затронуто: [backup-restore](pages/backup-restore.md), [alerting](pages/alerting.md).

## [2026-08-04] ingest | Зеркало пуша выкачено на прод

Выкатка зеркала (см. запись выше). Прод получил только `deploy/nginx/tu4ka.conf`
отдельным коммитом `21cae26` поверх `main`: остальной `dev` (бэкапы, алерты,
legal-страницы) к проду готов не был. Дерево коммита сверено с последним реально
выкаченным состоянием прода — приложение не изменилось, деплой свёлся к
`reload nginx`.

Проверка после выкатки: `last_ts` беты совпал с продом на первом же пуше,
`age_s` 31 с у обеих сред, ноль 401 в `journalctl -u tu4ka-beta`, ноль новых
записей в `error.log`, путь датчика на голом IP и `push.amqi.am` — 200, не 301.

Побочный след: `main` содержит коммит, которого нет в `dev`, так что следующий
`dev → main` придёт как non-ff и потребует явного слияния (конфликта не будет —
содержимое `tu4ka.conf` на обеих ветках идентично).

Затронуто: [nginx-tls-beta](pages/nginx-tls-beta.md).

## [2026-08-04] ingest | S1: раннер миграций и device_id в схеме

Схема перестала быть строкой в `db.py` и стала версионируемой цепочкой:
`PRAGMA user_version`, шаги в `server/migrations.py`, раннер в
`server/migrate.py`, применение отдельным процессом (`ExecStartPre`) до старта
uvicorn. Приложение теперь только сверяет версию и не стартует на чужой схеме.
Объём v2 сознательно урезан против карточки плана волн: заводим `devices`,
`device_id` в `measurements` и `measurements_raw`, а `users`/`sessions`/
`device_membership`/`device_latest`/`measurements_hourly` придут своими
миграциями там, где их пишут. Сессии идут последовательно, так что аргумент
«развести параллельные волны по номерам миграций» не работает.

Что выяснилось по ходу и стоило правок:

- **v1 сделан дословной копией старой схемы** — тогда боевая база с
  `user_version=0` структурно уже на ней, применение v1 к ней no-op, и отдельная
  ветка «определить легаси» не нужна;
- **`executescript()` рвёт транзакцию миграции** (делает COMMIT перед запуском)
  — отсюда форма шага «кортеж инструкций», а не одна строка SQL;
- **наивный порядок перестроения таблицы не работает**: `ALTER TABLE … RENAME`
  разбирает всю схему и спотыкается о FK `measurements_raw` на удалённую
  таблицу. Старая таблица уезжает под именем `measurements_v1`, новая создаётся
  сразу под финальным именем;
- **вынос `raw` экономит не диск, а чтения**: после `VACUUM` файл похудел на
  0.1 МБ, но горячая `measurements` теперь 1.0 МБ против 10.6 МБ у
  `measurements_raw`. Прикидка «освободится ~10 МБ» из плана была неверной,
  страница поправлена на измеренное;
- уникальный индекс `(device_id, ts)` сделал повторный пуш в ту же секунду
  ошибкой — приём переведён на `ON CONFLICT … DO UPDATE`, иначе прошивка
  получала бы 500 и теряла измерение безвозвратно.

Репетиция на снимке прода (14 899 строк) прошла: `integrity_check ok`,
`foreign_key_check` пуст, обе таблицы по 14 899 строк, сирот нет, повторный
прогон no-op. Сверка с живым продом по `/history?period=7d` — 56 корзин из 57
совпали байт в байт, разошлась только текущая наполняющаяся. Дашборд на
мигрированной базе рисуется целиком.

Разово снята копия боевой базы (`~/Documents/tu4ka-backups/`, 664 КБ) — правилу
«ничего в прод до релиза» это не противоречит, копия снимается `mode=ro`
и деплоем не является. Заодно зафиксировано: до релиза бэкап-таймер не стоит,
поэтому перед релизной выкладкой копию надо снять руками ещё раз.

Тестов стало 315 (было 294): миграции, geo, приём в новую схему.

Затронуто: новая [db-schema-migrations](pages/db-schema-migrations.md),
[requirements](pages/requirements.md) (новая секция M1–M9, правки P7/P10),
[index](index.md), [server-package-layout](pages/server-package-layout.md).
